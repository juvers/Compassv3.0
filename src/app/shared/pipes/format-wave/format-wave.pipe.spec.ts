import { FormatWavePipe } from './format-wave.pipe';

describe('FormatWavePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatWavePipe();
    expect(pipe).toBeTruthy();
  });
});
