import { ChooseImagePipe } from './choose-image.pipe';

describe('ChooseImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ChooseImagePipe();
    expect(pipe).toBeTruthy();
  });
});
